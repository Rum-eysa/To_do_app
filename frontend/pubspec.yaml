name: todo_app
description: A new Flutter project for Todo App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 📱 MEVCUT PAKETLERİN (korundu)
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_dotenv: ^5.1.0
  flutter_local_notifications: ^17.2.4
  flutter_timezone: ^3.0.1
  
  # 🆕 EKLENEN PAKETLER (sadece eksik olanlar)
  timezone: ^0.9.4              # Bildirim zamanlaması için ZORUNLU
  path_provider: ^2.1.1        # Bildirim ses dosyaları için
  permission_handler: ^11.3.0  # Bildirim izinleri için
  rxdart: ^0.27.7             # Bildirim akışları için
  android_alarm_manager_plus: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - .env
    # 🆕 BİLDİRİM SESLERİ İÇİN (opsiyonel)
    # - assets/sounds/notification.mp3